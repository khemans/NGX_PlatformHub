# Bootstrap: first init this root without dynamodb_table, then add locking after the table exists.
# Prefer a dedicated small state object for this root.

bucket  = "YOUR_ORG_TERRAFORM_STATE_BUCKET"
key     = "ngx-platform-hub/dev/infrastructure/dynamodb/terraform.tfstate"
region  = "us-east-1"
encrypt = true
