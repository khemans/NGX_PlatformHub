# Diagrams

| File | Description |
|------|----------------|
| [**`architecture.md`**](architecture.md) | High-level AWS topology (Mermaid). Renders on GitHub/GitLab; edit the fenced `mermaid` block when infra changes. |

To add a **draw.io** source later, place `architecture.drawio` (or `.xml`) in this folder and link it from `architecture.md` or this README.
